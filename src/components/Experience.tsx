const Experience = () => {
  return <div>Experience</div>;
  // sm: horizontal clickable slidebar
  // for each experience, show h2, span for timeline, and <ul> for description (create component)
  // for md and above: sidebar becomes left and right to each experience component
};

export default Experience;
